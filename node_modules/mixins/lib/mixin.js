util = require('util')

module.exports = {
  extend: function(base, mixin) {
    
    if(mixin instanceof Function && base instanceof Function) {    
      util.inherits(base, mixin);
    }

    for (var property in mixin) {
      base[property] = mixin[property];
    }
  }
}